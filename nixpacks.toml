[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["python3.11 seed_data.py"]

[start]
cmd = "python3.11 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
